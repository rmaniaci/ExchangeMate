# Uncomment the next line to define a global platform for your project
platform :ios, '11.3'

target 'ExchangeMate' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ExchangeMate
  pod 'Alamofire', '~> 4.7.1'
  pod 'SwiftyJSON'

  target 'ExchangeMateTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ExchangeMateUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
